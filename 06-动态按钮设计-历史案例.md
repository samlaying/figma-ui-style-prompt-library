# 动态按钮设计（历史案例）

> 对应课程：Figma 网页教学｜动态按钮设计，制作流畅的转场效果
>
> 状态：当前 Figma 文件中已删除，不属于现存画板清单。

## 本讲导读

制作一个带动态过渡效果的下载按钮：点击后绿色进度条从左至右滑过，随后出现打勾图标，并经历“放大 → 回弹”的动画。

## 一、搭建按钮基础形状

### 1. 绘制底色矩形

使用矩形工具绘制按钮形状，拖动四角圆角控制点，圆角可设置为约 53–57px。

### 2. 绘制进度条矩形

按住 Option / Alt 向下拖动复制按钮矩形，将复制矩形改为绿色，用作进度条和完成状态。

### 3. 组合并裁剪

1. 同时选中底色矩形和进度条矩形。
2. 使用 `⌘ Command + ⌥ Option + G`（Windows：Ctrl + Alt + G）包裹为 Frame。
3. 将 Frame 圆角设置为与原矩形一致，例如 57px。
4. 勾选 `Clip Content`，让溢出到 Frame 外部的进度条不可见。

### 4. 设置进度条初始位置

将绿色进度条向左拖出 Frame 边界，使其在初始状态不可见。确认它仍然属于 Clip Content Frame。

## 二、添加打勾图标

可以使用 Iconify 插件搜索 `tick`，选择圆圈打勾图标并放入按钮 Frame。

初始状态：

| 属性 | 初始值 |
| --- | --- |
| 缩放 | 0.1 |
| 透明度 | 0% |

## 三、添加文字和图层结构

按钮内添加 `Download` 文字，将打勾图标与文字归类。推荐结构：

```text
Frame（圆角 + Clip Content）
├── Button Base
├── Progress Bar（初始位于 Frame 左侧外部）
├── Check Icon（初始缩放 0.1，透明度 0%）
└── Download Label
```

## 四、六个动画关键帧

| 帧 | 进度条 | 打勾图标 | 文字 |
| --- | --- | --- | --- |
| 1 初始 | 左侧外部，不可见 | 缩放 0.1，透明度 0% | 显示 Download |
| 2 隐藏文字 | 仍在外部 | 缩放 0.1，透明度 0% | 缩放 0.1，透明度 0% |
| 3 开始进度 | 拉回 Frame 中间 | 缩放 0.1，透明度 0% | 隐藏 |
| 4 完成进度 | 完全覆盖按钮 | 缩放 0.1，透明度 0% | 隐藏 |
| 5 勾选放大 | 完全覆盖 | 恢复正常并略放大 | 隐藏 |
| 6 勾选回弹 | 完全覆盖 | 略小于帧 5，回弹到正常 | 隐藏 |

帧 3、帧 4 可以把进度条高度缩小，并将两端做成胶囊形，模拟进度条滑入。

## 五、创建组件集和 Prototype

1. 选中所有帧，点击 `Create Component Set`。
2. 原型连线如下：

| 连线 | 触发 | 动画 | 时长 | 延迟 |
| --- | --- | --- | --- | --- |
| 1 → 2 | On Click | Smart Animate | 300ms | — |
| 2 → 3 | After Delay | Smart Animate | 300ms | 1ms |
| 3 → 4 | After Delay | Smart Animate | 300ms | 1ms |
| 4 → 5 | After Delay | Smart Animate | 300ms | 1ms |
| 5 → 6 | After Delay | Smart Animate | 300ms | 1ms |

Smart Animate 依赖图层名称匹配，复制关键帧时不要修改 `Progress Bar`、`Check Icon`、`Download Label` 等名称。

## 完整 Figma 提示词

```text
制作一个 AI 职场导师品牌风格的动态下载按钮组件，使用轻盈的薰衣草蓝紫主色 #8B95F2、薄荷绿完成色 #6DD4A0、蜜桃粉作为少量提示色，按钮主圆角约 24–32px，整体柔和、专业、轻盈，不要做成厚重的游戏按钮。

请创建一个带 Clip Content 的圆角按钮 Frame。按钮底色使用 #8B95F2，内部创建一个同样圆角的绿色 Progress Bar，初始状态将进度条放到按钮左侧边界之外，确保仍然属于 Clip Content Frame。按钮内保留名为“Download Label”的文字、名为“Progress Bar”的绿色矩形，以及名为“Check Icon”的圆圈打勾图标，所有关键图层名称在复制状态时保持不变。

创建 6 个动画关键帧：
1）Initial：显示 Download，Progress Bar 在左侧外部不可见，Check Icon 缩放 0.1、透明度 0%；
2）Hide Label：Download Label 缩放 0.1、透明度 0%，进度条仍在外部；
3）Progress In：绿色进度条移动到按钮内部中间，形成开始滑入效果；
4）Complete：绿色进度条完全覆盖按钮内部；
5）Check Pop：进度完成后 Check Icon 以正常尺寸略放大的状态出现，透明度 100%；
6）Check Settle：Check Icon 从略大尺寸回弹到略小于放大态的正常尺寸，透明度保持 100%。

将 6 个状态创建为 Component Set。设置 Prototype：Initial → Hide Label 使用 On Click + Smart Animate + 300ms；后续每一段使用 After Delay 1ms + Smart Animate + 300ms，自动连续播放。确保 Clip Content、同名图层和按钮圆角都保持一致。最终效果应该是：点击 Download → 文字消失 → 绿色进度条从左向右滑入 → 进度完成 → 勾选放大出现 → 勾选回弹。
```
