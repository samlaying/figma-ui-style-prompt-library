# 网页波浪载入动画（历史案例）

> 对应课程：Figma 设计教学｜制作网页载入动画，有趣的流动效果
>
> 状态：当前 Figma 文件中已删除，不属于现存画板清单。

## 本讲导读

制作一个波浪式网页载入动画：中央圆形内的波浪从底部逐渐上涨、充满圆形、溢出并覆盖整个画面，最后透明过渡到完整网页内容。

## 一、准备页面素材

建立一个 1280 × 832 的页面 Frame，内部从下到上包含：

1. 背景图，例如海滩或冲浪图片。
2. 半透明黑色遮罩，用于降低背景亮度。
3. 页面主标题。
4. 副标题或说明文字。
5. 圆角按钮和按钮文字。

如果没有外部图片，可以用蓝色到沙黄色的渐变矩形模拟海滩背景，再叠加深色半透明遮罩。

## 二、制作波浪圆形遮色片

### 生成波浪

课程中可以使用 `Get the Waves` 外挂生成波浪，选择普通波浪、方形或尖角样式，并调整波浪数量后点击 Create。

在 Figma 中为波浪设置渐层色，左右色标均保持 100% 不透明。

### 创建第二个波浪

复制第一个波浪，执行 `Flip Horizontal`，让两个方向相反的波浪叠加，产生自然的流动感。

### 使用圆形遮色片

1. 绘制一个圆形。
2. 将两个波浪放置到圆形上方。
3. 选中圆形和两个波浪，执行 `Use as Mask`。
4. 调整波浪的初始位置：左右波浪都只在圆形底部露出一小部分。

## 三、四个关键帧

### 关键帧 1：波浪低位

- 圆形内的波浪在底部，只露出少量颜色。
- 圆形下方显示 `Loading`。
- 背景使用与最终页面同色的矩形或渐变。

### 关键帧 2：波浪充满圆形

1. 复制关键帧 1。
2. 将左侧波浪向上拉大，完全覆盖圆形。
3. 将右侧波浪也向上拉大，完全覆盖圆形。
4. 检查波浪之间是否有缝隙，必要时继续扩大。

### 关键帧 3：波浪溢出全屏

1. 复制关键帧 2。
2. 删除 `Loading` 文字。
3. 将圆形和内部波浪整体放大到超出页面边界。
4. 让波浪覆盖整个页面。

### 关键帧 4：显示最终页面

1. 复制关键帧 3。
2. 将最终页面素材放入并与页面对齐。
3. 将波浪圆形整体设置为完全透明。
4. 露出背景图、遮罩、标题、说明和按钮。

## 四、Prototype 连线

依次连接：`关键帧 1 → 关键帧 2 → 关键帧 3 → 关键帧 4`。

| 连线 | 触发 | 动画 | 缓动 | 时长 |
| --- | --- | --- | --- | --- |
| 1 → 2 | After Delay 1ms | Smart Animate | Linear | 2000ms |
| 2 → 3 | After Delay 1ms | Smart Animate | Linear | 1000ms |
| 3 → 4 | After Delay 1ms | Smart Animate | Linear | 1000ms |

所有图层需要保持同名，让 Smart Animate 识别位置、大小和透明度变化。动画节奏是先慢后快：波浪上涨用 2 秒，溢出和页面揭示各用 1 秒。

## 核心要点

- `Get the Waves` 用于快速生成波浪。
- 圆形 Mask 将波浪限制在圆形内部。
- 水平翻转第二个波浪，增加流动自然度。
- 4 帧分别表现低位、充满、溢出和页面揭示。
- `After Delay + Smart Animate` 让动画自动连续播放。

## 完整 Figma 提示词

```text
制作一个 AI 职场导师产品的网页波浪载入动画，整体使用“春天早晨的书房”视觉：轻盈、温暖、干净、具有流动感。创建一个 1280×832 风格的页面展示板，使用蓝色到沙黄色的海滩渐变作为背景图替代素材，并叠加低透明度深色遮罩；最终页面包含标题“Find your next wave”、副标题“A calmer way to move forward.”和一个薰衣草蓝紫圆角按钮“Explore”。

请制作 4 个同尺寸、同层级结构的顶层关键帧，并保持以下图层名称一致：Beach Background、Dark Overlay、Hero Title、Hero Copy、CTA、Wave Mask Viewport、Wave Circle Mask Group、Wave Circle Mask、Wave Left、Wave Right、Loading Label。圆形和波浪需要使用 Mask 或等效圆形裁切，确保波浪只在圆形范围内显示。

关键帧 1：Low Water。中央出现一个圆形，两个方向相反的波浪只在圆形底部露出一小部分，波浪使用 #8B95F2→#C4B5F7 和 #A8E6CF→柔和天蓝渐变；圆形下方显示 Loading，最终网页标题、描述和按钮隐藏。

关键帧 2：Fill Circle。保持圆形位置不变，将 Wave Left 和 Wave Right 同时向上拉大，让波浪完全充满圆形内部，不能出现缝隙。Loading 保持显示。

关键帧 3：Overflow。删除 Loading，将圆形和波浪整体放大到超出整个页面，让波浪覆盖完整画布，形成从圆形载入到全屏揭示的过渡。

关键帧 4：Final Page。保持背景图、遮罩、标题、说明和 CTA 的同名图层，显示完整网页内容；将 Wave Circle Mask Group 透明度设置为 0，露出最终页面。

Prototype 连接必须是 1→2→3→4，全部使用 After Delay 1ms + Smart Animate + Linear。1→2 时长 2000ms，2→3 时长 1000ms，3→4 时长 1000ms。确保 4 个关键帧是同一页面上的顶层 Frame，这样 NAVIGATE 原型目标可用。最终动效是：波浪低位 → 缓慢上涨并充满圆形 → 加速溢出全屏 → 透明过渡到最终网页。
```
