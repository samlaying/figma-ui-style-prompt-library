# 内容渐隐效果

> 对应课程：Figma 学 UI 设计技巧｜内容渐隐效果怎么做？
>
> 当前 Figma 画板：`内容渐隐效果 · Content Fade`（节点 `66:2`）

## 什么是内容渐隐效果

内容渐隐效果让 UI 元素在边缘逐渐消失，而不是被硬生生截断。它适用于横向滚动列表、Banner 底部过渡、Tab 栏边缘和毛玻璃渐隐。

实现方式有两种：透明渐变遮罩和透明渐变蒙版。

| 方法 | 原理 | 能否配合背景模糊 |
| --- | --- | :---: |
| 透明渐变遮罩 | 在元素上方叠加与背景同色的透明度渐变矩形 | 能 |
| 透明渐变蒙版 | 用带透明度渐变的几何图形裁切元素 | 不能 |

## 方法一：透明渐变遮罩

以 Banner 底部渐隐为例：

1. 在 Banner 上方绘制矩形，确保它只覆盖 Banner 的渐隐区域。
2. 将矩形填充色改为与页面背景完全一致。
3. 为矩形添加线性渐变，两个色标使用相同的背景色。
4. 将渐变旋转为上下方向。
5. 将上方或内容侧色标透明度设为 0，让渐变变成透明度变化而非颜色变化。
6. 拖拽渐变控制点，调整渐隐区域长度。
7. 如需毛玻璃效果，在遮罩矩形上添加 `Background Blur`。

> 关键：遮罩的颜色必须与背景完全一致，否则内容会出现一条明显的色带。

## 方法二：透明渐变蒙版

1. 在底层绘制对应需要裁切的几何图形。
2. 选中需要被影响的内容和几何图形，点击 `Use Mask`。
3. 调整蒙版图形的大小控制显示范围。
4. 为蒙版图形添加透明度渐变。
5. 蒙版对颜色不敏感，只使用透明度控制显示与消失。
6. 将一端色标透明度设为 0，另一端保持 100%。
7. 拖动纯色色标，控制完整显示区域和渐隐过渡区域。

## 遮罩 vs 蒙版

### 透明渐变遮罩

- 在元素上方叠加一个渐变矩形。
- 颜色必须匹配背景。
- 支持 `Background Blur`。
- 适合毛玻璃、背景模糊、Banner 融入页面。

### 透明渐变蒙版

- 用几何图形直接裁切元素。
- 颜色不重要，只对 Alpha 有反应。
- 不支持有效的背景模糊，因为蒙版位于底部，背后没有可供模糊的内容。
- 适合纯透明渐隐和不需要额外效果的内容裁切。

> 背景模糊只能使用遮罩实现。

## 完整 Figma 提示词

```text
制作一个“内容渐隐效果”教学展示板，整体延续 AI 职场导师的轻盈品牌风格：背景使用 #FAFBFE 或 #F3F4FE，卡片使用白色和 16px 圆角，文字使用 #2A2A34、#40404C 和 #62626E，边缘过渡必须柔和，不要出现硬裁切。

请在同一展示板上做左右两个对比区域。

左侧标题为“01 · Transparent Gradient Overlay”。放置一张 Banner 或横向内容卡片，在其底部叠加一个与页面背景颜色完全一致的渐变矩形。渐变上下方向，颜色两端相同，只改变透明度：内容一侧透明度为 0%，背景一侧透明度为 100%，让 Banner 内容逐渐融入 #FAFBFE 背景。渐变范围要足够长，不能出现一条突然消失的硬边。在这个遮罩矩形上添加轻微 Background Blur，展示遮罩方案可以配合背景模糊和毛玻璃效果。

右侧标题为“02 · Transparent Gradient Mask”。创建同样的 Banner 内容，再使用一个几何图形作为蒙版。蒙版使用透明度渐变，一端为 100% 显示，另一端为 0% 透明；颜色本身不承担视觉作用，只控制 Alpha。将内容和蒙版执行 Use Mask，展示纯透明渐隐效果。不要在这个蒙版上依赖 Background Blur，并在说明文字中标注蒙版不适合背景模糊场景。

在底部添加对比说明：遮罩颜色必须与背景匹配并支持 Background Blur；蒙版只依赖透明度，适合纯裁切。整体保证两张卡片之间有足够间距，渐隐方向、渐变控制点和过渡长度清晰可读。
```
