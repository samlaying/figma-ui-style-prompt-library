# 卡片高光效果

> 对应课程：Figma 学 UI 设计技巧｜卡片高光效果怎么做？
>
> 当前 Figma 画板：`卡片高光效果 · Card Highlights`（节点 `63:2`）

## 本讲导读

卡片高光效果由两部分组成：外部边缘高光和内部光影质感。外部高光使用渐变描边，内部质感可以做成金属、流光或镜面。高光效果的前提是深色背景，浅色背景上难以呈现层次。

## 一、外部边缘高光

1. 创建一个矩形作为卡片主体，填充使用深色调。
2. 为矩形添加描边。
3. 点击描边颜色，切换为线性渐变。
4. 将渐变两侧色标设置为白色或接近白色。
5. 在中间添加多个色标，相邻色标的透明度降低，形成明暗交替的光斑。
6. 拖动色标位置，调整明暗分布，直到边缘光影自然。
7. 描边颜色不局限于白色，也可以使用蓝色、黄色、橙色等，但背景需要与高光色协调。

## 二、内部光影质感

三种质感都可以先将素材或形状放在卡片上方，再使用蒙版将内容限制在卡片内部。

### 方法一：金属质感

1. 准备有明显高光纹理的金属贴图。
2. 将素材粘贴到卡片上方。
3. 选中素材和卡片底部图层，创建蒙版。
4. 调整素材大小、倾斜角度和缩放比例。
5. 将素材混合模式切换为 `Overlay` 或 `Soft Light`。
6. 调整卡片底色，观察不同颜色搭配下的金属质感。

### 方法二：流光质感

1. 在蒙版结构内使用钢笔工具绘制不规则流动形状。
2. 为形状添加纯色填充，再切换为渐变填充。
3. 为形状添加 `Layer Blur`，将模糊值拉高，形成柔和的流光。
4. 复制流光图层并翻转，设置不同透明度和渐变颜色。
5. 继续叠加多个不同方向的模糊形状，形成丰富的流动感。

### 方法三：镜面质感

1. 在蒙版结构内使用钢笔工具绘制一个斜切闭合路径。
2. 为路径添加线性渐变。
3. 将渐变两侧都设置为白色。
4. 大幅降低两侧色标透明度，一端稍亮，另一端更低。
5. 通过斜切区域模拟玻璃、镜面或锐利反射。

## 三种质感对比

| 质感 | 核心技法 | 关键操作 |
| --- | --- | --- |
| 金属 | 素材贴图 + 混合模式 | `Overlay` / `Soft Light` |
| 流光 | 钢笔形状 + Layer Blur | 多层模糊、翻转、调透明度 |
| 镜面 | 钢笔斜切 + 低透明渐变 | 白色渐变两端压低透明度 |

## 完整 Figma 提示词

```text
制作一个“卡片高光效果”展示板，整体必须放在深色背景上，使用 AI 职场导师的薰衣草蓝紫品牌光影，但视觉要高级、克制、柔和，不要变成强烈霓虹或游戏 UI。

请创建三张独立卡片，展示三种内部质感：金属质感、流光质感、镜面质感。每张卡片使用深色底色、16–20px 圆角和轻微品牌色阴影。

外部边缘高光：给每张卡片添加渐变描边，不要使用单一纯色描边。将渐变两侧设置为白色或浅蓝紫色，中间加入多个色标并降低相邻色标透明度，形成明暗交替的边缘光斑。光斑需要不均匀但自然，不能整圈均匀发亮。

金属质感卡片：在卡片上方放置有明显高光和纹理变化的金属贴图，将素材与卡片底部创建蒙版，并使用 Overlay 或 Soft Light 混合模式。可对素材进行倾斜、缩放和旋转，让金属纹理具有方向性。

流光质感卡片：在卡片内部用钢笔绘制 3–5 个不规则的流动形状，填充薰衣草蓝紫、薄荷绿或淡紫渐变，添加较高的 Layer Blur。复制并翻转部分流光图层，使用不同透明度和方向叠加，形成柔和的流动光带。

镜面质感卡片：在卡片内部绘制一个干净利落的斜切闭合路径，填充白色线性渐变。将渐变两端透明度大幅降低，一端略亮、另一端接近透明，模拟玻璃或镜面的锐利反射。镜面高光必须微妙，不要遮挡卡片内容。

三张卡片之间保持足够间距，标题和说明文字不能被高光覆盖。整体使用深色背景、低透明度蓝紫 Glow、柔和渐变和克制的高光。
```
