# 彩色弥散效果

> 对应课程：Figma 学 UI 设计技巧｜彩色弥散效果怎么做？
>
> 当前 Figma 画板：`彩色弥散效果 · Color Diffusion`（节点 `58:2`）

## 本讲导读

彩色弥散效果是应用界面顶部常见的、带有扩散和模糊效果的彩色背景。完整流程是：绘制彩色几何图形、添加图层模糊、叠加噪点纹理。

## 什么是彩色弥散效果

在顶部区域放置彩色几何图形，通过扩散和模糊形成柔和的色彩过渡背景。它适合 Hero、Banner、空状态、头像光晕等场景，视觉效果好且制作简单。

## 实现步骤

### 第一步：绘制彩色几何图形

1. 为画布设置大致的背景色，不建议使用纯白色，可使用 `#FAFBFE`、`#F3F4FE` 或品牌浅色。
2. 绘制多个大小不同的圆形，错落分布在画布的不同位置。
3. 为圆形设置不同颜色：左上角可使用蓝紫色，右上角可使用偏白色，其他区域使用薄荷绿、薰衣草或奶油黄。
4. 颜色之间的色差不要太大，否则模糊混合后会显得脏或突兀。
5. 可通过单个圆形的透明度微调最终混合效果。

### 第二步：添加图层模糊

1. 选中所有彩色圆形。
2. 在右侧属性栏点击 `Effect`，添加 `Layer Blur`。
3. 将模糊值拉到 200 以上，通常从 200–300 开始调整。
4. 观察颜色的扩散范围。如果颜色过脏，优先调整圆形颜色、透明度和位置，不要只继续增加模糊。

> 模糊值过小，扩散效果不明显；彩色弥散需要大胆使用较大的模糊值。

### 第三步：添加噪点纹理（可选）

1. 绘制一个与画布等大的矩形。
2. 为矩形添加 `Effect → Noise`。
3. 将图层混合模式切换为 `Soft Light`。
4. 将噪点透明度控制在 30%–50%，约 40% 通常比较自然。
5. 通过 Noise 设置调整颜色和密度。

噪点层是可选步骤：想要平滑、干净的界面可以不添加；想要颗粒感和层次感时再加入。

## 关键参数

| 步骤 | 操作 | 建议参数 |
| --- | --- | --- |
| 几何图形 | 多个不同大小的圆形错落分布 | 颜色低饱和、色差不要过大 |
| 图层模糊 | `Effect → Layer Blur` | 200–300 起步 |
| 噪点层 | `Noise → Soft Light` | 透明度 30%–50%，约 40% |

## 品牌色版本

- 主色：`#8B95F2`
- 薰衣草：`#C4B5F7`
- 薄荷绿：`#A8E6CF`
- 奶油黄：`#F9E4A7`
- 页面底色：`#FAFBFE`

## 完整 Figma 提示词

```text
制作一块适用于 AI 职场导师产品首页顶部的彩色弥散背景，整体气质是“春天早晨的书房”：轻盈、干净、温暖、通透，不要深夜霓虹感。

画布使用暖白偏蓝的背景 #FAFBFE。创建多个大小不同、位置错落的圆形几何图形，使用品牌色 #8B95F2（薰衣草蓝紫）、#C4B5F7（浅薰衣草）、#A8E6CF（薄荷绿）、#F9E4A7（奶油黄），颜色保持低饱和、低对比，避免相邻色块出现明显硬边。

将所有彩色圆形添加 Layer Blur，模糊值建议 200–300，使圆形边缘完全扩散并形成柔和的色彩过渡。根据最终画面调整每个圆形的位置、大小和透明度，让蓝紫色承担品牌识别，薄荷绿和奶油黄只作为轻微呼吸感点缀。

可选：叠加一层与画布等大的噪点纹理，使用 Noise 效果和 Soft Light 混合模式，透明度控制在 30%–50%，建议约 40%。噪点只能增加细腻颗粒感，不能抢过背景和页面文字。

最终效果需要适合 Hero、Banner 或顶部背景：颜色像被阳光照亮的薄雾，边缘柔和，没有明显的圆形轮廓、没有高饱和色块、没有黑色阴影。
```
